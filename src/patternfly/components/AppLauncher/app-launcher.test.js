const collapsed = require('./collapsed.hbs');
// const render = require('../../../../test/jest/render');

describe('app-launcher', () => {
  // afterEach(cleanup);

  describe('automated accessibility testing', () => {
    it('should have no DAP violations', () => {
      // console.log(collapsed);
      // const container = render.render(collapsed);
      // console.log(container);
      // await expect(container).toHaveNoDAPViolations(container);
      expect(3).toEqual(3);
    });
  });
});
